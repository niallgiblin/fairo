# Circuit research evidence (references only).
# Binary assets (schematics/PDFs) stay local: they are reference material,
# not build inputs, and the text extracts + report.md carry the values.
*.pdf
*.png
*.jpg
crops/
crops2/
tiles/
kt_images/
